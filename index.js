const divide = function (parameter1, parameter2) {
  return parameter1 / parameter2;
};
divide(2000, 100);

const square = (parameter5) => parameter5 ** 2;
square(2);

const add = (parameter3, parameter4) => {
  return parameter3 + parameter4;
};
add(2000, 100);
